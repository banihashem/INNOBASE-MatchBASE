# Autonomous organization registers

These JSON files are the machine-readable control plane for Slice 0. They are reviewed in Git and rendered read-only in the local dashboard. Evidence links point to exact local source paths and hashes; no protected source bytes are copied into the repository.

Unknown external state remains `UNKNOWN` or `BLOCKED`. Implementer self-assessment never authorizes a gate pass.
