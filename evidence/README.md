# Slice 0 evidence

This directory contains sanitized, repository-safe evidence derived from the current implementation. It contains no credentials, protected source contents, original intake text, or provider payloads.

- Dependency licenses are evaluated dynamically by `pnpm license:check`; no stale generated inventory is committed.
- No SBOM is claimed in Slice 0. A pinned, reproducible SBOM pipeline is a release-gate requirement for a later deployable slice.

The current dashboard snapshot contains source paths, metadata, and SHA-256 values only. It does not contain protected source excerpts.
