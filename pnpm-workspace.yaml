packages:
  - apps/*
  - packages/*

# MB-UX-GOV-003 L01: security patches for transitive dependencies.
# Remove a pin only after every parent resolves a patched version naturally.
overrides:
  fast-uri: 3.1.6
  js-yaml@>=4.0.0 <4.3.2: 4.3.2
  sharp: 0.35.4
