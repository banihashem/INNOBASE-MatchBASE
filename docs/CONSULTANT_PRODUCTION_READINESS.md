# Consultant production boundary

Activity: MB-UX-PILOT-001 L01. Canonical local authority: `C:/INNOBASE/MatchBASE/PROJECT_START_HERE.md`.

Source preparation does not establish production readiness. The combined production worker now polls the Consultant queue and preserves legacy research/PDF consumers. Its startup checks credentials, provider routes, current queue columns and minimum queue privileges. These checks do not establish all production database grants or successful live inference.

The production web process rejects both OpenRouter key names. Queue accelerators run only outside production; production jobs must be consumed by the worker. Initial interpretation, interpretation retry, correction preview and authenticated quote planning still contain synchronous web-side gateway operations. They require an additive durable command/result migration before the complete Consultant journey can run with worker-only OpenRouter credentials. Do not work around this blocker by injecting keys into web or silently downgrading the runtime environment.

The production web image includes the pinned Playwright closure and Chromium for V3 PDF rendering, with a non-root browser-to-PDF build check. A local development image build is not qualification of the production image, actual authenticated PDF route, object-storage permissions or production resource limits.

## Required qualification

1. Persist interpretation, correction and quotation commands before returning pending state. Preserve immutable request, profile/account, execution, version and quote-expiry bindings. Quotation must not dispatch paid research.
2. Demonstrate completion with no OpenRouter key or outbound OpenRouter request in the production web process. Exercise process restart, duplicate commands, lease expiry, cancellation and result ownership with disposable infrastructure and controlled providers first.
3. Configure explicit Google/OpenAI provider choices and qualified account-specific routes; extra model families require their own funding and routing acceptance. Missing or stale credentials/catalogs/policy fail closed.
4. Build and execute production web/worker images under their actual non-root identities. Test current migrations and role grants, real identity admission, complete Consultant journey, PDF output and storage access.
5. Qualify backup and artifact restoration, retention, monitoring, cumulative budgets and rollback. Real deployments and paid model qualification require the applicable approval.

Workflow mutations require session CSRF proof, same-origin admission and a fresh Idempotency-Key. Requiring that header alone does not establish persistent deduplication for every draft action. Run resources are scoped to both account and user profile; synthetic shared fixtures require explicit local/test fixture mode.

Recent-price extraction retains exact source dates and quotations. Validation records per-observation rejection reasons and the bounded source excerpts supplied to extraction. The additional validation checkpoint shares the original request ID; billing remains one ledger entry per provider request. Controlled tests do not establish live price coverage.
